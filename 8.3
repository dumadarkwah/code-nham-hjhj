#include <iostream>
using namespace std;

int main () {
    int n;
    cout<<"Nhap co phan tu: ";
    cin>>n;
    
    int arr[n];
    for(int i = 0; i < n; i++) {
        cout<<"So thu "<<i+1<<" la: ";
        cin>>arr[i];
        if (arr[i] % 2 == 0) {
        cout<<arr[i]<<" la so chan\n";
    }
     else {cout<<arr[i]<<" la so le\n";}
    }
   
    return 0;
}
