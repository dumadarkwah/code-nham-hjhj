#include <iostream>
using namespace std; 

int main () {
    //Khai bao bien
int luongCanBan = 650000;
float heSo;
int TNCT;

//Dieu kien tinh luong
if (TNCT < 12) {
    heSo = 1.92;
} else if (12 <= TNCT < 36) {
    heSo = 2.34;
} else if (36 <= TNCT < 60) {
    heSo = 3;
} else if (TNCT >= 60) {
    heSo = 4.5; 
}
//Khai bao cong thuc
int luong = heSo * luongCanBan;
cout<<"Thoi gian lam viec cua nhan vien(thang): ";
cin>>TNCT;
cout<<"luong nhan vien la: "<<luong; 
    return 0;
}
