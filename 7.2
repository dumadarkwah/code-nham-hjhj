#include <iostream>
using namespace std; 

int main () {
    /*Viết chương trình tính lương của nhân viên dựa theo thâm niên công
tác (TNCT) như sau:
Lương = hệ số * lương căn bản, trong đó lương căn bản là 650000
• Nếu TNCT < 12 tháng: hệ số = 1.92
• Nếu 12 <= TNCT < 36 tháng: hệ số = 2.34
• Nếu 36 <= TNCT < 60 tháng: hệ số = 3
• Nếu TNCT >= 60 tháng: hệ số = 4.5*/

int luong, luongCanBan = 650000;
float heSo;
int TNCT;

luong = heSo * luongCanBan;
if (TNCT < 12) {
    heSo = 1.92;
} else if (12 <= TNCT < 36) {
    heSo = 2.34;
} else if (36 <= TNCT < 60) {
    heSo = 3;
} else if (TNCT >= 60) {
    heSo = 4.5; 
}

    return 0;
}
