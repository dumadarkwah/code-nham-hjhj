/*Bài 1: kiểm tra số chẵn lẻ*/
#include <iostream>
using namespace std;

int main () {
    int n;
    cout<<"Nhap 1 so nguyen: ";
    cin>>n;
    
    if (n % 2 == 0) {
        cout<<n<<" la so chan";
    }
    else { cout<<n<<" la so le"; }
    return 0;
}
